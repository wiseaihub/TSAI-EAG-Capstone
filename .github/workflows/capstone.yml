name: Capstone CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  sanity-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Repository structure check
        run: |
          echo "Running basic capstone sanity checks..."
          ls -la
          echo "CI pipeline executed successfully."
